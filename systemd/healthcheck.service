[Unit]
Description=Host health check (RAID, SMART, disk, memory, containers, RAS)
After=docker.service
OnFailure=notify-failure@%n.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/healthcheck.sh
